projection implementation in class ZBP_C_J0FLIGHT unique;
strict ( 2 );
extensible;
use draft;
use side effects;

define behavior for ZC_J0FLIGHT alias FLIGHT
extensible
use etag
{
//  " Definir el campo Planetype como de solo lectura para este servicio
  field ( readonly ) Planetype;

//  " Comentar las operaciones de creación y eliminación
  // use create;
  use update;
  // use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}